//This will delete the line just deleted erb syntax is #{dom_id(user)}
<%flash_selector = "div#flash"%>
<% if not @special_partial_dot_key.nil? %>
  $('#special_partial_dot_key_<%=@special_partial_dot_key.id %>').remove();
<%end%>
$("<%=flash_selector%>").html("<%=j display_flashes%>");
timeoutID = window.setTimeout(clearFlash,20000);
function clearFlash(){
  $("<%=flash_selector%>").empty();
}