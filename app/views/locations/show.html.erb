<h3><%= "#{@location.to_s}" %></h3>
<h5>Modify this node</h5>
<ul>
  <li><%= button_to "Edit #{@location.to_s}", edit_location_path(@location), :method => :get %></li>
  <li><%= button_to "Delete #{@location.to_s}", location_path(@location), :method => :delete, :confirm => "Are you sure?", :title => "Confirm" %></li>
</ul>
<h5>add a child (if it is not a venue)</h5>
<ul>
  <li><%= button_to "Add an Area", new_child_area_path(@location), :method => :get %></li>
  <li><%= button_to "Add a Venue", new_child_venue_path(@location), :method => :get %></li>
  <li><%= button_to "Add an Organisation",new_child_organisation_path(@location), :method => :get %></li>
<p>
<a href="/locations"> Back </a>
</ul>
-# %h5 params #{params}

