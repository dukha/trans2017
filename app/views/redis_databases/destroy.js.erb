//This will delete the line just deleted erb syntax is #{dom_id(redis_database)}
<%flash_selector = "div#flash"%>
<% if not @redis_database.nil? %>
  $('#redis_database_<%=@redis_database.id %>').remove();
<%end%>
$("<%=flash_selector%>").html("<%=j display_flashes%>");
timeoutID = window.setTimeout(clearFlash,20000);
function clearFlash(){
  $("<%=flash_selector%>").empty();
}