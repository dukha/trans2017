<p class="pageheading"><%=t("headings.listing.heading", :model=>t($ARM + "server.other") )%></p>

<%= twill_paginate %>
<table class="data">
  <tr class="dataheader">

    <th class="linkheader"></th>
    <th class="linkheader"></th>
    
  </tr>

  
<% cell_class = "data" %>
<% link_cell_class="link"%>
  
<% @servers.each do |server| %>

 <tr class = "<%= cycle('dataeven', 'dataodd') %>" >

    
    <td class="<%= link_cell_class %>"><%= tlink_to  'edit'), edit_server_path(server) %></td>
    <td class="<%= link_cell_class %>"><%= tlink_to 'destroy'),  server , :confirm => 'delete.are_you_sure', :model =>  "server", :count=>1, :method => :delete %></td>
  </tr>
<% end %>
</table>
<%= twill_paginate %>

<br />
<%= link_to t('headings.new.heading', :model=> t($ARM + 'server', :count=>1)), new_server_path %>