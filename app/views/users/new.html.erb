<%= theading('new', :model=>'user', :count=>1)%>
<!-- this is for a sysadmin to reset another users password -->
<%= render 'form'%>
<br /><%= tlink_to('new_model', new_user_path, :model=> 'user', :count=>1) %> |
<%= tlink_to "back", users_path %>