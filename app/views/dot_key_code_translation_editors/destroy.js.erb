//This will delete the line just deleted erb syntax is #{dom_id(dot_key_translation_editor)}
<%flash_selector = "div#flash"%>
<% if not @dot_key_translation_editor.nil? %>
  $('#dot_key_translation_editor_<%=@dot_key_translation_editor.id %>').remove();
<%end%>
$("<%=flash_selector%>").html("<%=j display_flashes%>");
timeoutID = window.setTimeout(clearFlash,20000);
function clearFlash(){
  $("<%=flash_selector%>").empty();
}