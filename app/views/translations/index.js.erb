//This will delete the line just deleted erb syntax is #{dom_id(user)}
var identifier = '<%=dom_id(@translation)';
row = $('tr#' + identifier);
console.log("index.js.erb")
<%binding.pry%>
var flash_selector = "div#local-flash"; //.concat(@translation.id.to_s)%>  
row.find(flash_selector);
$("<%=flash_selector%>").html("<%=j display_flashes%>");
timeoutID = window.setTimeout(clearFlash,30000);
function clearFlash(){
  $("<%=flash_selector%>").empty();
}
