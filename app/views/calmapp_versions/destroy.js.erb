//This will delete the line just deleted erb syntax is #{dom_id(calmapp_version)}
var flash_selector = "div#flash";
<% if not @calmapp_version.nil? %>
  $('#calmapp_version_<%=@calmapp_version.id %>').remove();
<%end%>
$(flash_selector).html("<%=j display_flashes%>");
timeoutID = window.setTimeout(clearFlash,20000);
function clearFlash(){
  $(flash_selector).empty();
}