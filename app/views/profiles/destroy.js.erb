//This will delete the line just deleted erb syntax is #{dom_id(profile)}
var flash_selector = "div#flash";
<% if not @profile.nil? %>
  $('#profile_<%=@profile.id %>').remove();
<%end%>
$(flash_selector).html("<%=j display_flashes%>");
timeoutID = window.setTimeout(clearFlash,20000);
function clearFlash(){
  $(flash_selector).empty();
}